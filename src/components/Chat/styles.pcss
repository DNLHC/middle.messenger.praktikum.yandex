.Chat {
  display: flex;
  height: 100%;

  &_leftPanel {
    flex: 0 0 320px;
    width: 320px;
    border-right: 1px solid var(--color-border-default);
  }

  &_mainView {
    flex: 1 1 auto;
  }
}
